<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.Examenshopmobile" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="android:statusBarColor">@color/primaryColor</item>
        <item name="android:navigationBarColor">@color/primaryColor</item>
        <item name="android:colorBackground">@color/backgroundColor</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="buttonStyle">@style/ButtonStyle</item>
    </style>

    <style name="ButtonStyle" parent="Widget.Material3.Button">
        <item name="android:background">@drawable/rounded_button</item> <!-- Файл формы кнопки -->
        <item name="android:textColor">@color/buttonTextColor</item> <!-- Цвет текста кнопок -->
    </style>


    <style name="BottomNavigation.Light" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="android:background">@color/light_blurred_color</item>
    </style>

</resources>
